Run this shell command exactly:

```
claude plugin update executive-social-manager
```

Then tell the user:

"Plugin updated. **Restart Claude Code** to load the latest skills, commands, and exec profiles."

If the command fails, tell the user exactly what error appeared and suggest they contact Jesse.
